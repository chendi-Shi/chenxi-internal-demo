# Mission

## 使命

基于第一部分同事交付的 MCP / HTTP 检索服务契约，完成一个可以演示、解释和评测 Retrieval Policy 的最小可行系统：用户能够调整来源权重和新鲜度配置，下一次检索使用新策略，并能看见排序变化、分数组成、策略版本及 token 影响。

## 成功结果

最终 Demo 应证明以下闭环：

1. Dashboard 读取当前 `PolicyState`。
2. 用户修改来源权重或新鲜度参数。
3. Dashboard 使用 `expected_version` 保存策略。
4. 下一次 HTTP 与 MCP 检索由同一上游检索逻辑执行，并返回新的 `policy_version`。
5. UI 或评测报告能够解释排序变化，而不是在客户端重新排序。
6. ChatGPT 可以经 MCP 完成搜索、读取和引用；工具输出 token 有可复现的测量记录。

## 本仓库负责

- 上游 OpenAPI 契约的归档、校验和变更追踪。
- Retrieval Policy Dashboard 与类型安全的 HTTP 客户端。
- 策略修改的乐观锁、冲突提示和下一次查询生效验证。
- 来源权重、新鲜度与查询 metadata 过滤的实验界面。
- 固定虚构样例上的检索效果、延迟和 token 评测。
- MCP / ChatGPT 端到端验证记录及扩展建议。

## 本仓库不负责

- 系统原始内容文件、真实研报或真实业务数据。
- PDF 获取、解析、OCR、数据连接器和增量同步。
- 重写上游 MCP Server、存储、FTS5 索引或权威排序算法。
- 在 Dashboard 中复制、修正或覆盖后端评分。
- 生产级用户权限、企业认证、定时任务或分布式队列。
- 未经确认的公网部署、真实账号连接和外部数据写入。

## 不可破坏的边界

- `docs/contracts/openapi.json` 是机器可读接口事实来源。
- HTTP 与 MCP 必须共享上游同一套检索和策略状态。
- JSON 边界保持 `snake_case`，未知字段不得静默忽略。
- 策略保存必须携带 `expected_version`；409 冲突不能自动覆盖。
- 文档正文属于不可信数据，不能被当作系统指令执行。
- 原始内容缺失不阻塞本仓库开发；使用交付包内虚构样例和 mock。

## 完成定义

满足 [ROADMAP.md](ROADMAP.md) 各阶段退出条件，并形成以下证据：

- 契约测试通过；
- 相同 query 在策略修改前后的排序对比可复现；
- HTTP 与 MCP 返回一致的策略版本与排序语义；
- token 基线、优化方案和限制被量化记录；
- 已知接口缺口有明确结论：客户端规避、上游变更请求或后续阶段处理。
