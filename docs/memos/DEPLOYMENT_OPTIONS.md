# 部署选项备忘录

状态：仅记录未来选择，不属于当前阶段实现承诺。

## 当前决定

阶段 E 先使用局域网临时服务：Dashboard 由 Vite 监听 `0.0.0.0`，使用系统分配的临时端口，在受信任局域网内完成 fixture 页面验收。测试结束即停止进程，不配置公网入口。

2026-09-24 更新：开发调试使用 `npm run demo`，交付验收使用 `npm run preview:lan` 运行 `dist/` 生产包。两者均由系统选择临时端口；本次生产验收结束后已停止服务。这个选择足以支持当前本机/内网测试，不承诺固定端口、守护进程或公网可用性。

## 以后再决定

- 受控测试环境由谁托管，以及 HTTP API、MCP 与 Dashboard 是否同域。
- TLS、域名、反向代理和允许的 CORS Origin。
- Dashboard 服务端代理与 read/admin Token 的保管方式。
- ChatGPT 访问 MCP 所需的身份连接与网络边界。
- 正式构建的静态托管、容器化或内部平台选择。
- 日志、审计、健康检查、备份和回滚策略。

## 决策触发条件

只有在真实 MCP / ChatGPT 联调需要非 loopback 地址，或准备进入阶段 F 的可运行交付时，才评估以上选项。任何公网暴露、真实凭据或外部服务接入都需要单独确认。
